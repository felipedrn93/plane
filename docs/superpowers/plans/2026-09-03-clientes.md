# Módulo Clientes — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar um cadastro de Clientes (nome + N empresas com CNPJ) no menu principal do workspace e permitir vincular cada tarefa a um cliente, com filtro, agrupamento e coluna na planilha.

**Architecture:** Duas entidades novas no `plane.db` em escopo de workspace (`Client`, `ClientCompany`) e uma FK nullable `Issue.client`. O backend expõe CRUD em `/api/workspaces/<slug>/clients/` com `WorkspaceEntityPermission` (Convidado só lê). O frontend ganha um item de sidebar estático, rotas `/clients` e `/clients/:clientId`, um `ClientStore` MobX, um dropdown de propriedade e uma coluna de planilha — todos seguindo os padrões já usados pelos mods `parent-breadcrumb` e `tarefas-recorrentes`.

**Tech Stack:** Django 4 + DRF + Postgres (backend, ruff, pytest); React Router v7 + MobX + SWR + Tailwind (web); pnpm + Turbo; oxlint/oxfmt.

**Spec:** `docs/superpowers/specs/2026-09-03-clientes-cadastro-design.md`

---

## Correção em relação ao spec

O spec dizia que `client_id` precisaria entrar no `FIELD_MAPPER` de `plane/utils/grouper.py`. Isso está **errado**: o `FIELD_MAPPER` existe só para campos de array (`label_ids`, `assignee_ids`, `module_ids`). `client_id` é escalar, como `state_id` e `cycle_id` — basta entrar em `required_fields` e ganhar um branch em `issue_group_values`. O plano abaixo usa a versão correta.

## Estrutura de arquivos

### Backend (`apps/api/plane/`)

| Arquivo                                                                 | Responsabilidade                                                                                                   |
| ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `utils/cnpj.py` _(criar)_                                               | `normalize_cnpj` e `is_valid_cnpj` — puro, sem Django, para poder ser importado por serializers sem risco de ciclo |
| `db/models/client.py` _(criar)_                                         | Models `Client` e `ClientCompany`                                                                                  |
| `db/models/__init__.py` _(modificar)_                                   | Exportar os dois models                                                                                            |
| `db/models/issue.py` _(modificar)_                                      | Campo `Issue.client` + `"client": True` no default de display properties                                           |
| `db/migrations/0127_client_clientcompany_issue_client.py` _(criar)_     | Migration                                                                                                          |
| `app/serializers/client.py` _(criar)_                                   | `ClientCompanySerializer`, `ClientSerializer`                                                                      |
| `app/serializers/__init__.py` _(modificar)_                             | Exportar os serializers                                                                                            |
| `app/views/client/__init__.py` + `app/views/client/base.py` _(criar)_   | `ClientViewSet`, `ClientCompanyViewSet`                                                                            |
| `app/views/__init__.py` _(modificar)_                                   | Exportar as views                                                                                                  |
| `app/urls/client.py` _(criar)_                                          | Rotas                                                                                                              |
| `app/urls/__init__.py` _(modificar)_                                    | Registrar `client_urls`                                                                                            |
| `app/serializers/issue.py` _(modificar)_                                | `client` na allowlist de campos                                                                                    |
| `app/views/issue/base.py`, `app/views/issue/sub_issue.py` _(modificar)_ | `client_id` nas projeções `.values()`                                                                              |
| `utils/grouper.py` _(modificar)_                                        | `client_id` em `required_fields`; branch em `issue_group_values`                                                   |
| `utils/issue_filters.py` _(modificar)_                                  | `filter_client`                                                                                                    |
| `tests/unit/utils/test_cnpj.py` _(criar)_                               | Testes do validador                                                                                                |
| `tests/unit/views/test_client.py` _(criar)_                             | Testes de API (permissões, unicidade, exclusão bloqueada)                                                          |

### Frontend

| Arquivo                                                                                         | Responsabilidade                                                |
| ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| `packages/types/src/client.ts` _(criar)_                                                        | `TClient`, `TClientCompany`                                     |
| `packages/types/src/index.ts` _(modificar)_                                                     | `export * from "./client"`                                      |
| `packages/types/src/issues/issue.ts` _(modificar)_                                              | `TBaseIssue.client_id`                                          |
| `packages/types/src/view-props.ts` _(modificar)_                                                | `IIssueDisplayProperties.client` + `IIssueFilterOptions.client` |
| `packages/services/src/client/client.service.ts` + `index.ts` _(criar)_                         | `ClientService`                                                 |
| `packages/services/src/index.ts` _(modificar)_                                                  | `export * from "./client"`                                      |
| `packages/constants/src/workspace.ts` _(modificar)_                                             | Item `clients` na sidebar                                       |
| `packages/constants/src/issue/common.ts` _(modificar)_                                          | Display property, coluna da planilha, group-by                  |
| `apps/web/core/store/client.store.ts` _(criar)_                                                 | `ClientStore` MobX                                              |
| `apps/web/core/store/root.store.ts` _(modificar)_                                               | Registrar o store                                               |
| `apps/web/core/hooks/store/use-client.ts` _(criar)_                                             | Hook `useClient`                                                |
| `apps/web/app/routes/core.ts` _(modificar)_                                                     | Rotas `/clients`                                                |
| `apps/web/app/(all)/[workspaceSlug]/(projects)/clients/…` _(criar)_                             | Layout + páginas                                                |
| `apps/web/core/components/clients/…` _(criar)_                                                  | Lista, modal, detalhe                                           |
| `apps/web/core/components/dropdowns/client.tsx` _(criar)_                                       | Dropdown de propriedade                                         |
| `apps/web/core/components/issues/issue-layouts/spreadsheet/columns/client-column.tsx` _(criar)_ | Coluna da planilha                                              |
| `apps/web/ce/components/issues/issue-layouts/utils.tsx` _(modificar)_                           | Registro da coluna + ícone                                      |
| `apps/web/ce/components/workspace/sidebar/helper.tsx` _(modificar)_                             | Ícone da sidebar                                                |
| `apps/web/core/components/workspace/sidebar/sidebar-item.tsx` _(modificar)_                     | `"clients"` em `staticItems`                                    |
| `apps/web/core/store/issue/issue-details/issue.store.ts` _(modificar)_                          | `client_id` na allowlist                                        |
| `apps/web/core/store/issue/helpers/base-issues.store.ts` _(modificar)_                          | Mapas de group-by/filtro                                        |
| `packages/i18n/src/locales/*/…` _(modificar)_                                                   | Chaves novas                                                    |

---

## Fase 1 — Validador de CNPJ

### Task 1: Validador de CNPJ

**Files:**

- Create: `apps/api/plane/utils/cnpj.py`
- Test: `apps/api/plane/tests/unit/utils/test_cnpj.py`

- [x] **Step 1: Escrever o teste que falha**

Criar `apps/api/plane/tests/unit/utils/test_cnpj.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

import pytest

from plane.utils.cnpj import is_valid_cnpj, normalize_cnpj


@pytest.mark.unit
class TestNormalizeCnpj:
    def test_strips_mask(self):
        assert normalize_cnpj("11.222.333/0001-81") == "11222333000181"

    def test_keeps_digits_only(self):
        assert normalize_cnpj(" 11222333000181 ") == "11222333000181"

    def test_none_returns_empty(self):
        assert normalize_cnpj(None) == ""


@pytest.mark.unit
class TestIsValidCnpj:
    def test_valid_cnpj_with_mask(self):
        assert is_valid_cnpj("11.222.333/0001-81") is True

    def test_valid_cnpj_without_mask(self):
        assert is_valid_cnpj("11222333000181") is True

    def test_wrong_check_digits(self):
        assert is_valid_cnpj("11222333000182") is False

    def test_wrong_length(self):
        assert is_valid_cnpj("1122233300018") is False

    def test_all_repeated_digits_rejected(self):
        assert is_valid_cnpj("00000000000000") is False
        assert is_valid_cnpj("11111111111111") is False

    def test_empty_and_none(self):
        assert is_valid_cnpj("") is False
        assert is_valid_cnpj(None) is False

    def test_letters_rejected(self):
        assert is_valid_cnpj("11.222.333/0001-8A") is False
```

- [x] **Step 2: Rodar o teste e ver falhar**

```bash
cd apps/api && python -m pytest plane/tests/unit/utils/test_cnpj.py -v --reuse-db --nomigrations
```

Esperado: FAIL — `ModuleNotFoundError: No module named 'plane.utils.cnpj'`.

- [x] **Step 3: Implementar**

Criar `apps/api/plane/utils/cnpj.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

"""Normalização e validação de CNPJ.

Módulo puro (sem Django) para poder ser importado tanto pelos serializers
quanto pelos models sem risco de import circular — mesmo motivo que levou
`plane/utils/recurrence_validator.py` a existir (ver mods/tarefas-recorrentes.md).
"""

import re

CNPJ_LENGTH = 14

_FIRST_WEIGHTS = [5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]
_SECOND_WEIGHTS = [6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]


def normalize_cnpj(value):
    """Devolve apenas os dígitos do CNPJ. `None` vira string vazia."""
    if not value:
        return ""
    return re.sub(r"\D", "", str(value))


def _check_digit(digits, weights):
    total = sum(int(digit) * weight for digit, weight in zip(digits, weights))
    remainder = total % 11
    return 0 if remainder < 2 else 11 - remainder


def is_valid_cnpj(value):
    """True quando o CNPJ tem 14 dígitos e os dois verificadores conferem."""
    cnpj = normalize_cnpj(value)

    if len(cnpj) != CNPJ_LENGTH:
        return False

    # Sequências repetidas (00000000000000, 11111111111111, ...) passam no
    # cálculo dos dígitos mas não são CNPJs reais.
    if cnpj == cnpj[0] * CNPJ_LENGTH:
        return False

    if _check_digit(cnpj[:12], _FIRST_WEIGHTS) != int(cnpj[12]):
        return False

    return _check_digit(cnpj[:13], _SECOND_WEIGHTS) == int(cnpj[13])
```

- [x] **Step 4: Rodar o teste e ver passar**

```bash
cd apps/api && python -m pytest plane/tests/unit/utils/test_cnpj.py -v --reuse-db --nomigrations
```

Esperado: `11 passed`.

- [x] **Step 5: Commit**

```bash
git add apps/api/plane/utils/cnpj.py apps/api/plane/tests/unit/utils/test_cnpj.py
git commit -m "feat(clientes): validador de CNPJ"
```

---

## Fase 2 — Modelos e migration

### Task 2: Models `Client` e `ClientCompany`

**Files:**

- Create: `apps/api/plane/db/models/client.py`
- Modify: `apps/api/plane/db/models/__init__.py`
- Test: `apps/api/plane/tests/unit/models/test_client.py`

- [x] **Step 1: Escrever o teste que falha**

Criar `apps/api/plane/tests/unit/models/test_client.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

import pytest
from django.db.utils import IntegrityError

from plane.db.models import Client, ClientCompany


@pytest.mark.unit
@pytest.mark.django_db
class TestClientModel:
    def test_defaults_to_active(self, workspace):
        client = Client.objects.create(workspace=workspace, name="Acme")
        assert client.is_active is True

    def test_name_is_unique_per_workspace(self, workspace):
        Client.objects.create(workspace=workspace, name="Acme")
        with pytest.raises(IntegrityError):
            Client.objects.create(workspace=workspace, name="Acme")

    def test_companies_are_cascade_deleted(self, workspace):
        client = Client.objects.create(workspace=workspace, name="Acme")
        ClientCompany.objects.create(
            workspace=workspace, client=client, name="Acme SP", cnpj="11222333000181"
        )
        client.delete()
        assert ClientCompany.objects.filter(client_id=client.id).count() == 0


@pytest.mark.unit
@pytest.mark.django_db
class TestClientCompanyModel:
    def test_cnpj_is_unique_per_workspace(self, workspace):
        client_a = Client.objects.create(workspace=workspace, name="Acme")
        client_b = Client.objects.create(workspace=workspace, name="Globex")
        ClientCompany.objects.create(
            workspace=workspace, client=client_a, name="Acme SP", cnpj="11222333000181"
        )
        with pytest.raises(IntegrityError):
            ClientCompany.objects.create(
                workspace=workspace, client=client_b, name="Globex SP", cnpj="11222333000181"
            )
```

- [x] **Step 2: Rodar e ver falhar**

```bash
cd apps/api && python -m pytest plane/tests/unit/models/test_client.py -v --reuse-db
```

Esperado: FAIL — `ImportError: cannot import name 'Client' from 'plane.db.models'`.

- [x] **Step 3: Implementar os models**

Criar `apps/api/plane/db/models/client.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

# Django imports
from django.db import models
from django.db.models import Q
from django.db.models.functions import Lower

# Module imports
from .base import BaseModel


class Client(BaseModel):
    """Cliente do workspace. Agrupa uma ou mais empresas (CNPJs)."""

    workspace = models.ForeignKey("db.Workspace", on_delete=models.CASCADE, related_name="clients")
    name = models.CharField(max_length=255)
    is_active = models.BooleanField(default=True)

    class Meta:
        verbose_name = "Client"
        verbose_name_plural = "Clients"
        db_table = "clients"
        ordering = ("name",)
        constraints = [
            models.UniqueConstraint(
                Lower("name"),
                "workspace",
                condition=Q(deleted_at__isnull=True),
                name="unique_client_name_per_workspace_when_not_deleted",
            )
        ]

    def __str__(self):
        return self.name


class ClientCompany(BaseModel):
    """Empresa (CNPJ) pertencente a um cliente."""

    workspace = models.ForeignKey("db.Workspace", on_delete=models.CASCADE, related_name="client_companies")
    client = models.ForeignKey("db.Client", on_delete=models.CASCADE, related_name="companies")
    name = models.CharField(max_length=255)
    cnpj = models.CharField(max_length=14)

    class Meta:
        verbose_name = "Client Company"
        verbose_name_plural = "Client Companies"
        db_table = "client_companies"
        ordering = ("name",)
        constraints = [
            models.UniqueConstraint(
                fields=["workspace", "cnpj"],
                condition=Q(deleted_at__isnull=True),
                name="unique_client_company_cnpj_per_workspace_when_not_deleted",
            )
        ]

    def __str__(self):
        return f"{self.name} <{self.cnpj}>"
```

Em `apps/api/plane/db/models/__init__.py`, adicionar junto das outras importações (mantendo a ordem alfabética da vizinhança, logo antes de `from .cycle import ...`):

```python
from .client import Client, ClientCompany
```

- [x] **Step 4: Gerar a migration**

```bash
cd apps/api && python manage.py makemigrations db --settings=plane.settings.local --name client_clientcompany
```

Esperado: cria `plane/db/migrations/0127_client_clientcompany.py` com `CreateModel` para `Client` e `ClientCompany`. Confirmar que o arquivo foi criado:

```bash
ls apps/api/plane/db/migrations/ | tail -3
```

- [x] **Step 5: Rodar os testes e ver passar**

```bash
cd apps/api && python -m pytest plane/tests/unit/models/test_client.py -v --reuse-db --create-db
```

Esperado: `4 passed`.

- [x] **Step 6: Commit**

```bash
git add apps/api/plane/db/models/client.py apps/api/plane/db/models/__init__.py apps/api/plane/db/migrations/ apps/api/plane/tests/unit/models/test_client.py
git commit -m "feat(clientes): models Client e ClientCompany"
```

### Task 3: Campo `Issue.client`

**Files:**

- Modify: `apps/api/plane/db/models/issue.py`
- Test: `apps/api/plane/tests/unit/models/test_client.py`

- [x] **Step 1: Escrever o teste que falha**

Adicionar ao final de `apps/api/plane/tests/unit/models/test_client.py`:

```python
@pytest.mark.unit
@pytest.mark.django_db
class TestIssueClientField:
    def test_deleting_client_nulls_the_issue_field(self, workspace, create_user):
        from plane.db.models import Issue, Project, State

        client = Client.objects.create(workspace=workspace, name="Acme")
        project = Project.objects.create(
            name="Proj", identifier="PROJ", workspace=workspace, created_by=create_user
        )
        state = State.objects.get(project=project, group="backlog")
        issue = Issue.objects.create(name="Tarefa", project=project, workspace=workspace, state=state, client=client)

        Client.objects.filter(id=client.id).delete()
        issue.refresh_from_db()

        assert issue.client_id is None

    def test_display_properties_default_includes_client(self):
        from plane.db.models.issue import get_default_display_properties

        assert get_default_display_properties()["client"] is True
```

- [x] **Step 2: Rodar e ver falhar**

```bash
cd apps/api && python -m pytest plane/tests/unit/models/test_client.py -v --reuse-db
```

Esperado: FAIL — `TypeError: Issue() got unexpected keyword arguments: 'client'`.

- [x] **Step 3: Implementar**

Em `apps/api/plane/db/models/issue.py`, na classe `Issue`, adicionar o campo logo depois de `parent`:

```python
    client = models.ForeignKey(
        "db.Client",
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="issues",
    )
```

E em `get_default_display_properties()`, adicionar a chave (junto de `"parent_breadcrumb": True`):

```python
        "client": True,
```

- [x] **Step 4: Gerar a migration**

```bash
cd apps/api && python manage.py makemigrations db --settings=plane.settings.local --name issue_client
```

Esperado: cria `0128_issue_client.py` com `AddField`.

- [x] **Step 5: Rodar os testes e ver passar**

```bash
cd apps/api && python -m pytest plane/tests/unit/models/test_client.py -v --reuse-db --create-db
```

Esperado: `6 passed`.

- [x] **Step 6: Commit**

```bash
git add apps/api/plane/db/models/issue.py apps/api/plane/db/migrations/
git commit -m "feat(clientes): FK Issue.client e display property padrao"
```

---

## Fase 3 — API

### Task 4: Serializers

**Files:**

- Create: `apps/api/plane/app/serializers/client.py`
- Modify: `apps/api/plane/app/serializers/__init__.py`
- Test: `apps/api/plane/tests/unit/serializers/test_client.py`

- [x] **Step 1: Escrever o teste que falha**

Criar `apps/api/plane/tests/unit/serializers/test_client.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

import pytest

from plane.app.serializers import ClientCompanySerializer, ClientSerializer
from plane.db.models import Client, ClientCompany


@pytest.mark.unit
@pytest.mark.django_db
class TestClientCompanySerializer:
    def test_rejects_invalid_cnpj(self):
        serializer = ClientCompanySerializer(data={"name": "Acme SP", "cnpj": "11222333000182"})
        assert serializer.is_valid() is False
        assert "cnpj" in serializer.errors

    def test_normalizes_masked_cnpj(self):
        serializer = ClientCompanySerializer(data={"name": "Acme SP", "cnpj": "11.222.333/0001-81"})
        assert serializer.is_valid() is True
        assert serializer.validated_data["cnpj"] == "11222333000181"


@pytest.mark.unit
@pytest.mark.django_db
class TestClientSerializer:
    def test_serializes_nested_companies(self, workspace):
        client = Client.objects.create(workspace=workspace, name="Acme")
        ClientCompany.objects.create(
            workspace=workspace, client=client, name="Acme SP", cnpj="11222333000181"
        )
        data = ClientSerializer(client).data
        assert data["name"] == "Acme"
        assert len(data["companies"]) == 1
        assert data["companies"][0]["cnpj"] == "11222333000181"

    def test_rejects_blank_name(self):
        serializer = ClientSerializer(data={"name": "   "})
        assert serializer.is_valid() is False
        assert "name" in serializer.errors
```

- [x] **Step 2: Rodar e ver falhar**

```bash
cd apps/api && python -m pytest plane/tests/unit/serializers/test_client.py -v --reuse-db
```

Esperado: FAIL — `ImportError: cannot import name 'ClientSerializer'`.

- [x] **Step 3: Implementar**

Criar `apps/api/plane/app/serializers/client.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

# Third party imports
from rest_framework import serializers

# Module imports
from plane.db.models import Client, ClientCompany
from plane.utils.cnpj import is_valid_cnpj, normalize_cnpj

from .base import BaseSerializer


class ClientCompanySerializer(BaseSerializer):
    class Meta:
        model = ClientCompany
        fields = ["id", "name", "cnpj", "client", "created_at", "updated_at"]
        read_only_fields = ["id", "client", "created_at", "updated_at"]

    def validate_name(self, value):
        name = (value or "").strip()
        if not name:
            raise serializers.ValidationError("O nome da empresa é obrigatório.")
        return name

    def validate_cnpj(self, value):
        cnpj = normalize_cnpj(value)
        if not is_valid_cnpj(cnpj):
            raise serializers.ValidationError("CNPJ inválido.")
        return cnpj


class ClientSerializer(BaseSerializer):
    companies = ClientCompanySerializer(many=True, read_only=True)
    issue_count = serializers.IntegerField(read_only=True, default=0)

    class Meta:
        model = Client
        fields = [
            "id",
            "name",
            "is_active",
            "companies",
            "issue_count",
            "workspace",
            "created_at",
            "updated_at",
        ]
        read_only_fields = ["id", "workspace", "companies", "issue_count", "created_at", "updated_at"]

    def validate_name(self, value):
        name = (value or "").strip()
        if not name:
            raise serializers.ValidationError("O nome do cliente é obrigatório.")
        return name
```

Em `apps/api/plane/app/serializers/__init__.py`, adicionar:

```python
from .client import ClientCompanySerializer, ClientSerializer
```

- [x] **Step 4: Rodar os testes e ver passar**

```bash
cd apps/api && python -m pytest plane/tests/unit/serializers/test_client.py -v --reuse-db
```

Esperado: `4 passed`.

- [x] **Step 5: Commit**

```bash
git add apps/api/plane/app/serializers/client.py apps/api/plane/app/serializers/__init__.py apps/api/plane/tests/unit/serializers/test_client.py
git commit -m "feat(clientes): serializers de Client e ClientCompany"
```

### Task 5: ViewSets e rotas

**Files:**

- Create: `apps/api/plane/app/views/client/__init__.py`, `apps/api/plane/app/views/client/base.py`, `apps/api/plane/app/urls/client.py`
- Modify: `apps/api/plane/app/views/__init__.py`, `apps/api/plane/app/urls/__init__.py`
- Test: `apps/api/plane/tests/unit/views/test_client.py`

- [x] **Step 1: Escrever o teste que falha**

Criar `apps/api/plane/tests/unit/views/test_client.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

import pytest
from rest_framework import status

from plane.db.models import Client, ClientCompany, WorkspaceMember


@pytest.mark.unit
@pytest.mark.django_db
class TestClientEndpoints:
    def _url(self, workspace):
        return f"/api/workspaces/{workspace.slug}/clients/"

    def test_member_can_create_client(self, session_client, workspace):
        response = session_client.post(self._url(workspace), {"name": "Acme"}, format="json")
        assert response.status_code == status.HTTP_201_CREATED
        assert response.data["name"] == "Acme"
        assert response.data["is_active"] is True

    def test_list_returns_nested_companies_and_issue_count(self, session_client, workspace):
        client = Client.objects.create(workspace=workspace, name="Acme")
        ClientCompany.objects.create(
            workspace=workspace, client=client, name="Acme SP", cnpj="11222333000181"
        )
        response = session_client.get(self._url(workspace))
        assert response.status_code == status.HTTP_200_OK
        assert len(response.data[0]["companies"]) == 1
        assert response.data[0]["issue_count"] == 0

    def test_guest_cannot_create_client(self, session_client, workspace, create_user):
        WorkspaceMember.objects.filter(workspace=workspace, member=create_user).update(role=5)
        response = session_client.post(self._url(workspace), {"name": "Acme"}, format="json")
        assert response.status_code == status.HTTP_403_FORBIDDEN

    def test_guest_can_list_clients(self, session_client, workspace, create_user):
        Client.objects.create(workspace=workspace, name="Acme")
        WorkspaceMember.objects.filter(workspace=workspace, member=create_user).update(role=5)
        response = session_client.get(self._url(workspace))
        assert response.status_code == status.HTTP_200_OK

    def test_duplicate_name_is_rejected(self, session_client, workspace):
        Client.objects.create(workspace=workspace, name="Acme")
        response = session_client.post(self._url(workspace), {"name": "acme"}, format="json")
        assert response.status_code == status.HTTP_400_BAD_REQUEST

    def test_delete_without_issues_succeeds(self, session_client, workspace):
        client = Client.objects.create(workspace=workspace, name="Acme")
        response = session_client.delete(f"{self._url(workspace)}{client.id}/")
        assert response.status_code == status.HTTP_204_NO_CONTENT

    def test_delete_with_issues_is_blocked(self, session_client, workspace, create_user):
        from plane.db.models import Issue, Project, State

        client = Client.objects.create(workspace=workspace, name="Acme")
        project = Project.objects.create(
            name="Proj", identifier="PROJ", workspace=workspace, created_by=create_user
        )
        state = State.objects.get(project=project, group="backlog")
        Issue.objects.create(name="Tarefa", project=project, workspace=workspace, state=state, client=client)

        response = session_client.delete(f"{self._url(workspace)}{client.id}/")
        assert response.status_code == status.HTTP_400_BAD_REQUEST
        assert Client.objects.filter(id=client.id).exists()


@pytest.mark.unit
@pytest.mark.django_db
class TestClientCompanyEndpoints:
    def test_member_can_add_company(self, session_client, workspace):
        client = Client.objects.create(workspace=workspace, name="Acme")
        url = f"/api/workspaces/{workspace.slug}/clients/{client.id}/companies/"
        response = session_client.post(
            url, {"name": "Acme SP", "cnpj": "11.222.333/0001-81"}, format="json"
        )
        assert response.status_code == status.HTTP_201_CREATED
        assert response.data["cnpj"] == "11222333000181"

    def test_invalid_cnpj_is_rejected(self, session_client, workspace):
        client = Client.objects.create(workspace=workspace, name="Acme")
        url = f"/api/workspaces/{workspace.slug}/clients/{client.id}/companies/"
        response = session_client.post(url, {"name": "Acme SP", "cnpj": "11222333000182"}, format="json")
        assert response.status_code == status.HTTP_400_BAD_REQUEST

    def test_duplicate_cnpj_in_workspace_is_rejected(self, session_client, workspace):
        client_a = Client.objects.create(workspace=workspace, name="Acme")
        client_b = Client.objects.create(workspace=workspace, name="Globex")
        ClientCompany.objects.create(
            workspace=workspace, client=client_a, name="Acme SP", cnpj="11222333000181"
        )
        url = f"/api/workspaces/{workspace.slug}/clients/{client_b.id}/companies/"
        response = session_client.post(
            url, {"name": "Globex SP", "cnpj": "11222333000181"}, format="json"
        )
        assert response.status_code == status.HTTP_400_BAD_REQUEST
```

- [x] **Step 2: Rodar e ver falhar**

```bash
cd apps/api && python -m pytest plane/tests/unit/views/test_client.py -v --reuse-db
```

Esperado: FAIL — todas as requisições retornam 404 (rota inexistente).

- [x] **Step 3: Implementar as views**

Criar `apps/api/plane/app/views/client/base.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

# Django imports
from django.db.models import Count, Q
from django.db.models.functions import Lower

# Third party imports
from rest_framework import status
from rest_framework.response import Response

# Module imports
from plane.app.permissions import WorkspaceEntityPermission
from plane.app.serializers import ClientCompanySerializer, ClientSerializer
from plane.app.views.base import BaseViewSet
from plane.db.models import Client, ClientCompany, Workspace


class ClientViewSet(BaseViewSet):
    serializer_class = ClientSerializer
    model = Client
    permission_classes = [WorkspaceEntityPermission]

    def get_queryset(self):
        return (
            super()
            .get_queryset()
            .filter(workspace__slug=self.kwargs.get("slug"))
            .select_related("workspace")
            .prefetch_related("companies")
            .annotate(issue_count=Count("issues", filter=Q(issues__deleted_at__isnull=True), distinct=True))
            .order_by("name")
        )

    def list(self, request, slug):
        clients = self.get_queryset()
        if request.query_params.get("is_active") == "true":
            clients = clients.filter(is_active=True)
        return Response(ClientSerializer(clients, many=True).data, status=status.HTTP_200_OK)

    def create(self, request, slug):
        workspace = Workspace.objects.get(slug=slug)
        serializer = ClientSerializer(data=request.data)
        if not serializer.is_valid():
            return Response(serializer.errors, status=status.HTTP_400_BAD_REQUEST)

        if self._name_taken(workspace, serializer.validated_data["name"]):
            return Response(
                {"name": ["Já existe um cliente com esse nome neste workspace."]},
                status=status.HTTP_400_BAD_REQUEST,
            )

        serializer.save(workspace_id=workspace.id)
        return Response(serializer.data, status=status.HTTP_201_CREATED)

    def partial_update(self, request, slug, pk):
        client = self.get_queryset().get(pk=pk)
        serializer = ClientSerializer(client, data=request.data, partial=True)
        if not serializer.is_valid():
            return Response(serializer.errors, status=status.HTTP_400_BAD_REQUEST)

        name = serializer.validated_data.get("name")
        if name and self._name_taken(client.workspace, name, exclude_id=client.id):
            return Response(
                {"name": ["Já existe um cliente com esse nome neste workspace."]},
                status=status.HTTP_400_BAD_REQUEST,
            )

        serializer.save()
        return Response(serializer.data, status=status.HTTP_200_OK)

    def destroy(self, request, slug, pk):
        client = self.get_queryset().get(pk=pk)
        if client.issue_count > 0:
            return Response(
                {
                    "error": "Este cliente está vinculado a tarefas. Inative-o em vez de excluir.",
                    "issue_count": client.issue_count,
                },
                status=status.HTTP_400_BAD_REQUEST,
            )
        client.delete()
        return Response(status=status.HTTP_204_NO_CONTENT)

    def _name_taken(self, workspace, name, exclude_id=None):
        queryset = Client.objects.filter(workspace=workspace).annotate(lower_name=Lower("name")).filter(
            lower_name=name.lower()
        )
        if exclude_id:
            queryset = queryset.exclude(id=exclude_id)
        return queryset.exists()


class ClientCompanyViewSet(BaseViewSet):
    serializer_class = ClientCompanySerializer
    model = ClientCompany
    permission_classes = [WorkspaceEntityPermission]

    def get_queryset(self):
        return (
            super()
            .get_queryset()
            .filter(workspace__slug=self.kwargs.get("slug"), client_id=self.kwargs.get("client_id"))
            .order_by("name")
        )

    def create(self, request, slug, client_id):
        client = Client.objects.get(workspace__slug=slug, pk=client_id)
        serializer = ClientCompanySerializer(data=request.data)
        if not serializer.is_valid():
            return Response(serializer.errors, status=status.HTTP_400_BAD_REQUEST)

        if self._cnpj_taken(client.workspace_id, serializer.validated_data["cnpj"]):
            return Response(
                {"cnpj": ["Este CNPJ já está cadastrado neste workspace."]},
                status=status.HTTP_400_BAD_REQUEST,
            )

        serializer.save(workspace_id=client.workspace_id, client_id=client.id)
        return Response(serializer.data, status=status.HTTP_201_CREATED)

    def partial_update(self, request, slug, client_id, pk):
        company = self.get_queryset().get(pk=pk)
        serializer = ClientCompanySerializer(company, data=request.data, partial=True)
        if not serializer.is_valid():
            return Response(serializer.errors, status=status.HTTP_400_BAD_REQUEST)

        cnpj = serializer.validated_data.get("cnpj")
        if cnpj and self._cnpj_taken(company.workspace_id, cnpj, exclude_id=company.id):
            return Response(
                {"cnpj": ["Este CNPJ já está cadastrado neste workspace."]},
                status=status.HTTP_400_BAD_REQUEST,
            )

        serializer.save()
        return Response(serializer.data, status=status.HTTP_200_OK)

    def destroy(self, request, slug, client_id, pk):
        company = self.get_queryset().get(pk=pk)
        company.delete()
        return Response(status=status.HTTP_204_NO_CONTENT)

    def _cnpj_taken(self, workspace_id, cnpj, exclude_id=None):
        queryset = ClientCompany.objects.filter(workspace_id=workspace_id, cnpj=cnpj)
        if exclude_id:
            queryset = queryset.exclude(id=exclude_id)
        return queryset.exists()
```

Criar `apps/api/plane/app/views/client/__init__.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

from .base import ClientCompanyViewSet, ClientViewSet
```

Em `apps/api/plane/app/views/__init__.py`, adicionar:

```python
from .client import ClientCompanyViewSet, ClientViewSet
```

Criar `apps/api/plane/app/urls/client.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

from django.urls import path

from plane.app.views import ClientCompanyViewSet, ClientViewSet

urlpatterns = [
    path(
        "workspaces/<str:slug>/clients/",
        ClientViewSet.as_view({"get": "list", "post": "create"}),
        name="clients",
    ),
    path(
        "workspaces/<str:slug>/clients/<uuid:pk>/",
        ClientViewSet.as_view({"get": "retrieve", "patch": "partial_update", "delete": "destroy"}),
        name="client-detail",
    ),
    path(
        "workspaces/<str:slug>/clients/<uuid:client_id>/companies/",
        ClientCompanyViewSet.as_view({"get": "list", "post": "create"}),
        name="client-companies",
    ),
    path(
        "workspaces/<str:slug>/clients/<uuid:client_id>/companies/<uuid:pk>/",
        ClientCompanyViewSet.as_view({"patch": "partial_update", "delete": "destroy"}),
        name="client-company-detail",
    ),
]
```

Em `apps/api/plane/app/urls/__init__.py`, adicionar o import junto dos demais e a entrada na lista:

```python
from .client import urlpatterns as client_urls
```

```python
    *client_urls,
```

- [x] **Step 4: Rodar os testes e ver passar**

```bash
cd apps/api && python -m pytest plane/tests/unit/views/test_client.py -v --reuse-db
```

Esperado: `10 passed`.

- [x] **Step 5: Commit**

```bash
git add apps/api/plane/app/views/client/ apps/api/plane/app/views/__init__.py apps/api/plane/app/urls/client.py apps/api/plane/app/urls/__init__.py apps/api/plane/tests/unit/views/test_client.py
git commit -m "feat(clientes): endpoints de CRUD de clientes e empresas"
```

---

## Fase 4 — Integração do campo `client` nas tarefas (backend)

### Task 6: Filtro `client` nas issues

**Files:**

- Modify: `apps/api/plane/utils/issue_filters.py`
- Test: `apps/api/plane/tests/unit/utils/test_issue_filters_client.py`

- [x] **Step 1: Escrever o teste que falha**

Criar `apps/api/plane/tests/unit/utils/test_issue_filters_client.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

import pytest

from plane.utils.issue_filters import issue_filters

CLIENT_ID = "6a1f7f9e-9b2b-4a4a-8f6e-2b1c3d4e5f60"


@pytest.mark.unit
class TestFilterClient:
    def test_get_single_client(self):
        assert issue_filters({"client": CLIENT_ID}, "GET") == {"client_id__in": [CLIENT_ID]}

    def test_get_none_means_unassigned(self):
        assert issue_filters({"client": "None"}, "GET") == {"client_id__isnull": True}

    def test_post_takes_list(self):
        assert issue_filters({"client": [CLIENT_ID]}, "POST") == {"client_id__in": [CLIENT_ID]}

    def test_absent_key_is_ignored(self):
        assert issue_filters({}, "GET") == {}
```

- [x] **Step 2: Rodar e ver falhar**

```bash
cd apps/api && python -m pytest plane/tests/unit/utils/test_issue_filters_client.py -v --reuse-db --nomigrations
```

Esperado: FAIL — `assert {} == {'client_id__in': [...]}`.

- [x] **Step 3: Implementar**

Em `apps/api/plane/utils/issue_filters.py`, adicionar a função logo depois de `filter_logged_by`:

```python
def filter_client(params, issue_filter, method, prefix=""):
    if method == "GET":
        clients = [item for item in params.get("client").split(",") if item != "null"]
        if "None" in clients:
            issue_filter[f"{prefix}client_id__isnull"] = True
            clients = [item for item in clients if item != "None"]
        clients = filter_valid_uuids(clients)
        if len(clients) and "" not in clients:
            issue_filter[f"{prefix}client_id__in"] = clients
    else:
        if params.get("client", None) and len(params.get("client")) and params.get("client") != "null":
            issue_filter[f"{prefix}client_id__in"] = params.get("client")
    return issue_filter
```

E registrar no dicionário `ISSUE_FILTER` dentro de `issue_filters`, depois de `"module": filter_module,`:

```python
        "client": filter_client,
```

- [x] **Step 4: Rodar e ver passar**

```bash
cd apps/api && python -m pytest plane/tests/unit/utils/test_issue_filters_client.py -v --reuse-db --nomigrations
```

Esperado: `4 passed`.

- [x] **Step 5: Commit**

```bash
git add apps/api/plane/utils/issue_filters.py apps/api/plane/tests/unit/utils/test_issue_filters_client.py
git commit -m "feat(clientes): filtro de issues por cliente"
```

### Task 7: `client_id` nas projeções e no agrupamento

**Files:**

- Modify: `apps/api/plane/utils/grouper.py`, `apps/api/plane/app/serializers/issue.py`, `apps/api/plane/app/views/issue/base.py`, `apps/api/plane/app/views/issue/sub_issue.py`

Este é o pitfall das "shadow allowlists" documentado em `mods/tarefas-recorrentes.md`: cada lugar que projeta issues lista os campos manualmente. Se `client_id` faltar em qualquer um deles, o campo some naquela tela.

- [x] **Step 1: Escrever o teste que falha**

Criar `apps/api/plane/tests/unit/utils/test_grouper_client.py`:

```python
# Copyright (c) 2023-present Plane Software, Inc. and contributors
# SPDX-License-Identifier: AGPL-3.0-only
# See the LICENSE file for details.

import pytest

from plane.db.models import Client, Issue, Project, State
from plane.utils.grouper import issue_group_values, issue_on_results


@pytest.mark.unit
@pytest.mark.django_db
class TestGrouperClient:
    def test_issue_on_results_includes_client_id(self, workspace, create_user):
        client = Client.objects.create(workspace=workspace, name="Acme")
        project = Project.objects.create(
            name="Proj", identifier="PROJ", workspace=workspace, created_by=create_user
        )
        state = State.objects.get(project=project, group="backlog")
        Issue.objects.create(name="Tarefa", project=project, workspace=workspace, state=state, client=client)

        rows = issue_on_results(Issue.objects.all(), group_by=None, sub_group_by=None)

        assert rows[0]["client_id"] == client.id

    def test_issue_group_values_lists_active_clients_plus_none(self, workspace):
        active = Client.objects.create(workspace=workspace, name="Acme")
        Client.objects.create(workspace=workspace, name="Inativa", is_active=False)

        values = issue_group_values(field="client_id", slug=workspace.slug)

        assert active.id in values
        assert "None" in values
```

- [x] **Step 2: Rodar e ver falhar**

```bash
cd apps/api && python -m pytest plane/tests/unit/utils/test_grouper_client.py -v --reuse-db
```

Esperado: FAIL — `KeyError: 'client_id'`.

- [x] **Step 3: Implementar**

Em `apps/api/plane/utils/grouper.py`:

1. Adicionar `Client` ao import de `plane.db.models` (é a lista logo no topo do arquivo).
2. Em `issue_on_results`, adicionar `"client_id"` a `required_fields`, logo depois de `"cycle_id"`.
3. Em `issue_group_values`, adicionar o branch depois do branch de `cycle_id`:

```python
    if field == "client_id":
        queryset = Client.objects.filter(workspace__slug=slug, is_active=True).values_list("id", flat=True)
        return list(queryset) + ["None"]
```

Em `apps/api/plane/app/serializers/issue.py`:

4. Adicionar `"client"` a `IssueSerializer.Meta.fields`, junto de `"parent"`.
5. Em `IssueListDetailSerializer.to_representation`, adicionar `"client": str(instance.client_id) if instance.client_id else None`.

Em `apps/api/plane/app/views/issue/base.py`, adicionar `"client_id"` (logo depois de `"cycle_id"`) em:

6. o `.values(...)` de `IssueListEndpoint.get`
7. o `.values(...)` da resposta pós-create de `IssueViewSet.create`
8. o `required_fields` de `IssuePaginatedViewSet.list`

Em `apps/api/plane/app/views/issue/sub_issue.py`:

9. o `.values(...)` de `SubIssuesEndpoint.get`

- [x] **Step 4: Rodar e ver passar**

```bash
cd apps/api && python -m pytest plane/tests/unit/utils/test_grouper_client.py -v --reuse-db
```

Esperado: `2 passed`.

- [x] **Step 5: Verificar que nenhuma allowlist ficou para trás**

```bash
grep -rn "cycle_id" apps/api/plane/app/views/issue/base.py apps/api/plane/app/views/issue/sub_issue.py apps/api/plane/utils/grouper.py
```

Esperado: toda linha que lista `"cycle_id"` como string em uma projeção também lista `"client_id"`. Corrigir qualquer uma que não liste.

- [x] **Step 6: Rodar a suíte unitária inteira do backend**

```bash
cd apps/api && python -m pytest -m unit --reuse-db -q
```

Esperado: tudo passando, sem regressões.

- [x] **Step 7: Commit**

```bash
git add apps/api/plane/utils/grouper.py apps/api/plane/app/serializers/issue.py apps/api/plane/app/views/issue/ apps/api/plane/tests/unit/utils/test_grouper_client.py
git commit -m "feat(clientes): client_id nas projecoes e no agrupamento de issues"
```

---

## Fase 5 — Tipos, service e store

### Task 8: Tipos compartilhados

**Files:**

- Create: `packages/types/src/client.ts`
- Modify: `packages/types/src/index.ts`, `packages/types/src/issues/issue.ts`, `packages/types/src/view-props.ts`

- [x] **Step 1: Criar o arquivo de tipos**

Criar `packages/types/src/client.ts`:

```ts
/**
 * Copyright (c) 2023-present Plane Software, Inc. and contributors
 * SPDX-License-Identifier: AGPL-3.0-only
 * See the LICENSE file for details.
 */

export type TClientCompany = {
  id: string;
  name: string;
  /** Apenas dígitos (14 caracteres). A máscara é aplicada na exibição. */
  cnpj: string;
  client: string;
  created_at: string;
  updated_at: string;
};

export type TClient = {
  id: string;
  name: string;
  is_active: boolean;
  companies: TClientCompany[];
  issue_count: number;
  workspace: string;
  created_at: string;
  updated_at: string;
};
```

- [x] **Step 2: Exportar e estender os tipos existentes**

Em `packages/types/src/index.ts`, adicionar:

```ts
export * from "./client";
```

Em `packages/types/src/issues/issue.ts`, dentro de `TBaseIssue`, logo depois de `cycle_id`:

```ts
client_id: string | null;
```

Em `packages/types/src/view-props.ts`, dentro de `IIssueDisplayProperties` (junto de `parent_breadcrumb`):

```ts
  client?: boolean;
```

E dentro de `IIssueFilterOptions`:

```ts
  client?: string[] | null;
```

- [x] **Step 3: Verificar os tipos**

```bash
pnpm turbo run check:types --filter=@plane/types
```

Esperado: sem erros.

- [x] **Step 4: Commit**

```bash
git add packages/types/src/
git commit -m "feat(clientes): tipos TClient e TClientCompany"
```

### Task 9: `ClientService`

**Files:**

- Create: `packages/services/src/client/client.service.ts`, `packages/services/src/client/index.ts`
- Modify: `packages/services/src/index.ts`

- [x] **Step 1: Criar o service**

Criar `packages/services/src/client/client.service.ts`:

```ts
/**
 * Copyright (c) 2023-present Plane Software, Inc. and contributors
 * SPDX-License-Identifier: AGPL-3.0-only
 * See the LICENSE file for details.
 */

import { API_BASE_URL } from "@plane/constants";
import type { TClient, TClientCompany } from "@plane/types";
import { APIService } from "../api.service";

export class ClientService extends APIService {
  constructor(BASE_URL?: string) {
    super(BASE_URL || API_BASE_URL);
  }

  async list(workspaceSlug: string): Promise<TClient[]> {
    return this.get(`/api/workspaces/${workspaceSlug}/clients/`)
      .then((response) => response?.data)
      .catch((error) => {
        throw error?.response?.data;
      });
  }

  async create(workspaceSlug: string, data: Partial<TClient>): Promise<TClient> {
    return this.post(`/api/workspaces/${workspaceSlug}/clients/`, data)
      .then((response) => response?.data)
      .catch((error) => {
        throw error?.response?.data;
      });
  }

  async update(workspaceSlug: string, clientId: string, data: Partial<TClient>): Promise<TClient> {
    return this.patch(`/api/workspaces/${workspaceSlug}/clients/${clientId}/`, data)
      .then((response) => response?.data)
      .catch((error) => {
        throw error?.response?.data;
      });
  }

  async destroy(workspaceSlug: string, clientId: string): Promise<void> {
    return this.delete(`/api/workspaces/${workspaceSlug}/clients/${clientId}/`)
      .then((response) => response?.data)
      .catch((error) => {
        throw error?.response?.data;
      });
  }

  async createCompany(workspaceSlug: string, clientId: string, data: Partial<TClientCompany>): Promise<TClientCompany> {
    return this.post(`/api/workspaces/${workspaceSlug}/clients/${clientId}/companies/`, data)
      .then((response) => response?.data)
      .catch((error) => {
        throw error?.response?.data;
      });
  }

  async updateCompany(
    workspaceSlug: string,
    clientId: string,
    companyId: string,
    data: Partial<TClientCompany>
  ): Promise<TClientCompany> {
    return this.patch(`/api/workspaces/${workspaceSlug}/clients/${clientId}/companies/${companyId}/`, data)
      .then((response) => response?.data)
      .catch((error) => {
        throw error?.response?.data;
      });
  }

  async destroyCompany(workspaceSlug: string, clientId: string, companyId: string): Promise<void> {
    return this.delete(`/api/workspaces/${workspaceSlug}/clients/${clientId}/companies/${companyId}/`)
      .then((response) => response?.data)
      .catch((error) => {
        throw error?.response?.data;
      });
  }
}
```

Criar `packages/services/src/client/index.ts`:

```ts
/**
 * Copyright (c) 2023-present Plane Software, Inc. and contributors
 * SPDX-License-Identifier: AGPL-3.0-only
 * See the LICENSE file for details.
 */

export * from "./client.service";
```

Em `packages/services/src/index.ts`, adicionar:

```ts
export * from "./client";
```

- [x] **Step 2: Verificar os tipos**

```bash
pnpm turbo run check:types --filter=@plane/services
```

Esperado: sem erros.

- [x] **Step 3: Commit**

```bash
git add packages/services/src/
git commit -m "feat(clientes): ClientService"
```

### Task 10: `ClientStore` e hook

**Files:**

- Create: `apps/web/core/store/client.store.ts`, `apps/web/core/hooks/store/use-client.ts`
- Modify: `apps/web/core/store/root.store.ts`

- [x] **Step 1: Criar o store**

Criar `apps/web/core/store/client.store.ts`:

```ts
/**
 * Copyright (c) 2023-present Plane Software, Inc. and contributors
 * SPDX-License-Identifier: AGPL-3.0-only
 * See the LICENSE file for details.
 */

import { action, computed, makeObservable, observable, runInAction } from "mobx";
import { computedFn } from "mobx-utils";
// plane imports
import { ClientService } from "@plane/services";
import type { TClient, TClientCompany } from "@plane/types";

export interface IClientStore {
  // observables
  loader: boolean;
  clientMap: Record<string, TClient>;
  // computed
  clients: TClient[];
  activeClients: TClient[];
  // helpers
  getClientById: (clientId: string | null | undefined) => TClient | undefined;
  // actions
  fetchClients: (workspaceSlug: string) => Promise<TClient[]>;
  createClient: (workspaceSlug: string, data: Partial<TClient>) => Promise<TClient>;
  updateClient: (workspaceSlug: string, clientId: string, data: Partial<TClient>) => Promise<TClient>;
  deleteClient: (workspaceSlug: string, clientId: string) => Promise<void>;
  createCompany: (workspaceSlug: string, clientId: string, data: Partial<TClientCompany>) => Promise<TClientCompany>;
  updateCompany: (
    workspaceSlug: string,
    clientId: string,
    companyId: string,
    data: Partial<TClientCompany>
  ) => Promise<TClientCompany>;
  deleteCompany: (workspaceSlug: string, clientId: string, companyId: string) => Promise<void>;
}

export class ClientStore implements IClientStore {
  loader: boolean = false;
  clientMap: Record<string, TClient> = {};

  clientService: ClientService;

  constructor() {
    makeObservable(this, {
      loader: observable.ref,
      clientMap: observable,
      clients: computed,
      activeClients: computed,
      fetchClients: action,
      createClient: action,
      updateClient: action,
      deleteClient: action,
      createCompany: action,
      updateCompany: action,
      deleteCompany: action,
    });
    this.clientService = new ClientService();
  }

  get clients(): TClient[] {
    return Object.values(this.clientMap).sort((a, b) => a.name.localeCompare(b.name, "pt-BR"));
  }

  get activeClients(): TClient[] {
    return this.clients.filter((client) => client.is_active);
  }

  getClientById = computedFn((clientId: string | null | undefined) =>
    clientId ? this.clientMap[clientId] : undefined
  );

  fetchClients = async (workspaceSlug: string): Promise<TClient[]> => {
    try {
      runInAction(() => {
        this.loader = true;
      });
      const response = await this.clientService.list(workspaceSlug);
      runInAction(() => {
        this.clientMap = response.reduce<Record<string, TClient>>((acc, client) => {
          acc[client.id] = client;
          return acc;
        }, {});
        this.loader = false;
      });
      return response;
    } catch (error) {
      runInAction(() => {
        this.loader = false;
      });
      throw error;
    }
  };

  createClient = async (workspaceSlug: string, data: Partial<TClient>): Promise<TClient> => {
    const response = await this.clientService.create(workspaceSlug, data);
    runInAction(() => {
      this.clientMap[response.id] = response;
    });
    return response;
  };

  updateClient = async (workspaceSlug: string, clientId: string, data: Partial<TClient>): Promise<TClient> => {
    const previous = this.clientMap[clientId];
    runInAction(() => {
      if (previous) this.clientMap[clientId] = { ...previous, ...data } as TClient;
    });
    try {
      const response = await this.clientService.update(workspaceSlug, clientId, data);
      runInAction(() => {
        this.clientMap[clientId] = { ...response, companies: previous?.companies ?? response.companies };
      });
      return response;
    } catch (error) {
      runInAction(() => {
        if (previous) this.clientMap[clientId] = previous;
      });
      throw error;
    }
  };

  deleteClient = async (workspaceSlug: string, clientId: string): Promise<void> => {
    const previous = this.clientMap[clientId];
    runInAction(() => {
      delete this.clientMap[clientId];
    });
    try {
      await this.clientService.destroy(workspaceSlug, clientId);
    } catch (error) {
      runInAction(() => {
        if (previous) this.clientMap[clientId] = previous;
      });
      throw error;
    }
  };

  createCompany = async (
    workspaceSlug: string,
    clientId: string,
    data: Partial<TClientCompany>
  ): Promise<TClientCompany> => {
    const response = await this.clientService.createCompany(workspaceSlug, clientId, data);
    runInAction(() => {
      const client = this.clientMap[clientId];
      if (client) client.companies = [...client.companies, response];
    });
    return response;
  };

  updateCompany = async (
    workspaceSlug: string,
    clientId: string,
    companyId: string,
    data: Partial<TClientCompany>
  ): Promise<TClientCompany> => {
    const response = await this.clientService.updateCompany(workspaceSlug, clientId, companyId, data);
    runInAction(() => {
      const client = this.clientMap[clientId];
      if (client) client.companies = client.companies.map((company) => (company.id === companyId ? response : company));
    });
    return response;
  };

  deleteCompany = async (workspaceSlug: string, clientId: string, companyId: string): Promise<void> => {
    const client = this.clientMap[clientId];
    const previous = client?.companies ?? [];
    runInAction(() => {
      if (client) client.companies = previous.filter((company) => company.id !== companyId);
    });
    try {
      await this.clientService.destroyCompany(workspaceSlug, clientId, companyId);
    } catch (error) {
      runInAction(() => {
        if (client) client.companies = previous;
      });
      throw error;
    }
  };
}
```

- [x] **Step 2: Registrar no root store**

Em `apps/web/core/store/root.store.ts`:

Import (junto dos outros, em ordem alfabética entre `./cycle_filter.store` e `./dashboard.store`):

```ts
import type { IClientStore } from "./client.store";
import { ClientStore } from "./client.store";
```

Propriedade da classe (depois de `label: ILabelStore;`):

```ts
client: IClientStore;
```

Instanciação — adicionar a linha nos **dois** lugares, `constructor()` e `resetOnSignOut()`, depois de `this.label = new LabelStore(this);`:

```ts
this.client = new ClientStore();
```

- [x] **Step 3: Criar o hook**

Criar `apps/web/core/hooks/store/use-client.ts`:

```ts
/**
 * Copyright (c) 2023-present Plane Software, Inc. and contributors
 * SPDX-License-Identifier: AGPL-3.0-only
 * See the LICENSE file for details.
 */

import { useContext } from "react";
// mobx store
import { StoreContext } from "@/lib/store-context";
// types
import type { IClientStore } from "@/store/client.store";

export const useClient = (): IClientStore => {
  const context = useContext(StoreContext);
  if (context === undefined) throw new Error("useClient must be used within StoreProvider");
  return context.client;
};
```

- [x] **Step 4: Verificar os tipos**

```bash
pnpm turbo run check:types --filter=web
```

Esperado: sem erros novos.

- [x] **Step 5: Commit**

```bash
git add apps/web/core/store/client.store.ts apps/web/core/store/root.store.ts apps/web/core/hooks/store/use-client.ts
git commit -m "feat(clientes): ClientStore e hook useClient"
```

---

## Fase 6 — Sidebar, rotas e telas

### Task 11: Item "Clientes" na sidebar

**Files:**

- Modify: `packages/constants/src/workspace.ts`, `apps/web/ce/components/workspace/sidebar/helper.tsx`, `apps/web/core/components/workspace/sidebar/sidebar-item.tsx`

- [x] **Step 1: Adicionar a entrada de navegação**

Em `packages/constants/src/workspace.ts`, dentro de `WORKSPACE_SIDEBAR_STATIC_NAVIGATION_ITEMS`, adicionar logo depois do bloco `home`:

```ts
  clients: {
    key: "clients",
    labelTranslationKey: "clients.title",
    href: `/clients/`,
    access: [EUserWorkspaceRoles.ADMIN, EUserWorkspaceRoles.MEMBER, EUserWorkspaceRoles.GUEST],
    highlight: (pathname: string, url: string) => pathname.includes(url),
  },
```

E em `WORKSPACE_SIDEBAR_STATIC_NAVIGATION_ITEMS_LINKS`, adicionar depois de `home` (é essa ordem que coloca "Clientes" abaixo de "Página Inicial"):

```ts
export const WORKSPACE_SIDEBAR_STATIC_NAVIGATION_ITEMS_LINKS: IWorkspaceSidebarNavigationItem[] = [
  WORKSPACE_SIDEBAR_STATIC_NAVIGATION_ITEMS["home"],
  WORKSPACE_SIDEBAR_STATIC_NAVIGATION_ITEMS["clients"],
];
```

- [x] **Step 2: Adicionar o ícone**

Em `apps/web/ce/components/workspace/sidebar/helper.tsx`, adicionar o import do lucide e o `case`:

```tsx
import { Building2 } from "lucide-react";
```

```tsx
    case "clients":
      return <Building2 className={cn("size-4 flex-shrink-0", className)} />;
```

- [x] **Step 3: Liberar o item como estático**

Em `apps/web/core/components/workspace/sidebar/sidebar-item.tsx`, adicionar `"clients"` ao array `staticItems`, depois de `"home"`:

```tsx
const staticItems = [
  "home",
  "clients",
  "pi_chat",
  "projects",
  "your_work",
  "stickies",
  "drafts",
  ...(additionalStaticItems || []),
];
```

- [x] **Step 4: Verificação manual** _(pendente de confirmação do usuário no browser)_

```bash
pnpm dev --filter=web
```

Abrir `http://localhost:3000`, entrar em um workspace. Esperado: item "clients.title" (chave crua — o i18n vem na Task 18) aparece na sidebar logo abaixo de "Página Inicial", com o ícone de prédio. Clicar leva a 404 por enquanto (rota vem na próxima task).

- [x] **Step 5: Commit**

```bash
git add packages/constants/src/workspace.ts apps/web/ce/components/workspace/sidebar/helper.tsx apps/web/core/components/workspace/sidebar/sidebar-item.tsx
git commit -m "feat(clientes): item Clientes na sidebar do workspace"
```

### Task 12: Rotas e casca das páginas

**Files:**

- Create: `apps/web/app/(all)/[workspaceSlug]/(projects)/clients/layout.tsx`, `apps/web/app/(all)/[workspaceSlug]/(projects)/clients/page.tsx`, `apps/web/app/(all)/[workspaceSlug]/(projects)/clients/[clientId]/page.tsx`
- Modify: `apps/web/app/routes/core.ts`

- [x] **Step 1: Copiar o padrão do layout de stickies**

```bash
cat "apps/web/app/(all)/[workspaceSlug]/(projects)/stickies/layout.tsx"
```

Criar `apps/web/app/(all)/[workspaceSlug]/(projects)/clients/layout.tsx` com a mesma estrutura, trocando o header/título para Clientes. O layout de stickies renderiza um `AppHeader` + `ContentWrapper` + `<Outlet />`; reproduzir isso apontando para um `ClientsHeader` próprio criado no mesmo arquivo (breadcrumb simples com o texto "Clientes").

- [x] **Step 2: Criar as páginas**

`apps/web/app/(all)/[workspaceSlug]/(projects)/clients/page.tsx`:

```tsx
/**
 * Copyright (c) 2023-present Plane Software, Inc. and contributors
 * SPDX-License-Identifier: AGPL-3.0-only
 * See the LICENSE file for details.
 */

import { observer } from "mobx-react";
// components
import { ClientsListRoot } from "@/components/clients";

const ClientsPage = observer(function ClientsPage() {
  return <ClientsListRoot />;
});

export default ClientsPage;
```

`apps/web/app/(all)/[workspaceSlug]/(projects)/clients/[clientId]/page.tsx`:

```tsx
/**
 * Copyright (c) 2023-present Plane Software, Inc. and contributors
 * SPDX-License-Identifier: AGPL-3.0-only
 * See the LICENSE file for details.
 */

import { observer } from "mobx-react";
import { useParams } from "next/navigation";
// components
import { ClientDetailRoot } from "@/components/clients";

const ClientDetailPage = observer(function ClientDetailPage() {
  const { clientId } = useParams();
  if (!clientId) return null;
  return <ClientDetailRoot clientId={clientId.toString()} />;
});

export default ClientDetailPage;
```

- [x] **Step 3: Registrar as rotas**

Em `apps/web/app/routes/core.ts`, adicionar logo antes do bloco `// Stickies`:

```ts
        // Clients
        layout("./(all)/[workspaceSlug]/(projects)/clients/layout.tsx", [
          route(":workspaceSlug/clients", "./(all)/[workspaceSlug]/(projects)/clients/page.tsx"),
          route(
            ":workspaceSlug/clients/:clientId",
            "./(all)/[workspaceSlug]/(projects)/clients/[clientId]/page.tsx"
          ),
        ]),
```

- [x] **Step 4: Commit (a compilação só fecha depois da Task 13)**

Não commitar ainda — os componentes `ClientsListRoot` e `ClientDetailRoot` são criados na próxima task. Seguir direto para a Task 13 e commitar as duas juntas.

### Task 13: Componentes de lista, modal e detalhe

**Files:**

- Create: `apps/web/core/components/clients/index.ts`, `clients-list-root.tsx`, `client-form-modal.tsx`, `client-detail-root.tsx`, `cnpj.ts`

- [x] **Step 1: Helper de máscara/validação de CNPJ no front**

Criar `apps/web/core/components/clients/cnpj.ts`:

```ts
/**
 * Copyright (c) 2023-present Plane Software, Inc. and contributors
 * SPDX-License-Identifier: AGPL-3.0-only
 * See the LICENSE file for details.
 */

const CNPJ_LENGTH = 14;
const FIRST_WEIGHTS = [5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2];
const SECOND_WEIGHTS = [6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2];

export const normalizeCnpj = (value: string | null | undefined): string => (value ?? "").replace(/\D/g, "");

/** Formata para 00.000.000/0000-00, tolerando entradas parciais (usado no onChange do input). */
export const formatCnpj = (value: string | null | undefined): string => {
  const digits = normalizeCnpj(value).slice(0, CNPJ_LENGTH);
  return digits
    .replace(/^(\d{2})(\d)/, "$1.$2")
    .replace(/^(\d{2})\.(\d{3})(\d)/, "$1.$2.$3")
    .replace(/\.(\d{3})(\d)/, ".$1/$2")
    .replace(/(\d{4})(\d)/, "$1-$2");
};

const checkDigit = (digits: string, weights: number[]): number => {
  const total = digits.split("").reduce((sum, digit, index) => sum + Number(digit) * weights[index], 0);
  const remainder = total % 11;
  return remainder < 2 ? 0 : 11 - remainder;
};

export const isValidCnpj = (value: string | null | undefined): boolean => {
  const cnpj = normalizeCnpj(value);
  if (cnpj.length !== CNPJ_LENGTH) return false;
  if (cnpj === cnpj[0].repeat(CNPJ_LENGTH)) return false;
  if (checkDigit(cnpj.slice(0, 12), FIRST_WEIGHTS) !== Number(cnpj[12])) return false;
  return checkDigit(cnpj.slice(0, 13), SECOND_WEIGHTS) === Number(cnpj[13]);
};
```

- [x] **Step 2: Modal de cadastro/edição**

Criar `apps/web/core/components/clients/client-form-modal.tsx`. Requisitos concretos:

- Props: `{ isOpen: boolean; onClose: () => void; clientId?: string }`.
- Usa `ModalCore` de `@plane/ui` (mesmo componente usado pelos outros modais de workspace — conferir o import exato em `apps/web/core/components/project/create-project-modal.tsx`).
- `react-hook-form` com `useFieldArray` para a lista de empresas; campos: `name` (obrigatório, `required: true`), e para cada empresa `companies[i].name` (obrigatório) e `companies[i].cnpj` (validado com `isValidCnpj`, mensagem `t("clients.invalid_cnpj")`).
- Input de CNPJ aplica `formatCnpj` no `onChange` e envia `normalizeCnpj` no submit.
- Toggle de status usando `ToggleSwitch` de `@plane/ui`, rotulado com `clients.status.active` / `clients.status.inactive`. Só aparece na edição (na criação o cliente nasce ativo).
- Submit: no modo criação chama `createClient` e depois `createCompany` para cada linha; no modo edição chama `updateClient` e faz o diff das empresas (criar as sem `id`, atualizar as alteradas, remover as que sumiram da lista).
- Erros da API são exibidos com `setToast({ type: TOAST_TYPE.ERROR, title: ..., message: ... })`, lendo a primeira mensagem de `error.name` / `error.cnpj` quando existir.

- [x] **Step 3: Lista**

Criar `apps/web/core/components/clients/clients-list-root.tsx`. Requisitos concretos:

- `observer`, usa `useClient()` e `useParams()`.
- `useSWR(workspaceSlug ? "WORKSPACE_CLIENTS_" + workspaceSlug : null, () => fetchClients(workspaceSlug))`.
- Estado local: `searchQuery` (string) e `showInactive` (boolean, default `false`).
- Filtra: por `showInactive` (`client.is_active || showInactive`) e por `searchQuery` casando com `client.name` ou com o CNPJ normalizado de qualquer empresa.
- Renderiza tabela com colunas Nome (link para `/${workspaceSlug}/clients/${client.id}`), Empresas (`client.companies.length`), Tarefas (`client.issue_count`), Status (badge Ativo/Inativo) e um menu `CustomMenu` com Editar / Excluir.
- Botão "Novo cliente" no topo, escondido quando o usuário não tem permissão de escrita — usar `useUserPermissions().allowPermissions([EUserWorkspaceRoles.ADMIN, EUserWorkspaceRoles.MEMBER], EUserPermissionsLevel.WORKSPACE, workspaceSlug)`, mesmo padrão de `sidebar-item.tsx`.
- Excluir: chama `deleteClient`; no `catch`, se a resposta tiver `issue_count`, mostra toast de erro com `t("clients.delete_blocked")` e oferece o botão "Inativar" que chama `updateClient(workspaceSlug, id, { is_active: false })`.
- Estado vazio quando não há clientes: texto `t("clients.empty")` + o botão de criar.

- [x] **Step 4: Detalhe** _(desvio aprovado: a aba "Tarefas" virou um botão "Ver N tarefas" que leva a `/{slug}/workspace-views/all-issues/?client={clientId}` — o `AllIssueLayoutRoot` é acoplado ao `globalViewId` da rota e ao `useGlobalView`, então embutir a lista exigiria refatorá-lo; a view global já lê filtros da query string via `routeFilters`)_

Criar `apps/web/core/components/clients/client-detail-root.tsx`. Requisitos concretos:

- Props: `{ clientId: string }`. `observer`, usa `useClient()`.
- Mesmo `useSWR` da lista para garantir que o `clientMap` esteja populado em acesso direto pela URL.
- Cabeçalho: nome do cliente, badge de status, botão "Editar" que abre o `ClientFormModal` com o `clientId`.
- Bloco "Empresas": tabela com Nome e CNPJ formatado por `formatCnpj`.
- Aba "Tarefas": renderiza o layout global de work items filtrado por cliente. Reusar o mesmo componente que `workspace-views/[globalViewId]/page.tsx` usa para renderizar a lista, passando o filtro `{ client: [clientId] }`. Inspecionar esse arquivo antes de implementar:

```bash
cat "apps/web/app/(all)/[workspaceSlug]/(projects)/workspace-views/[globalViewId]/page.tsx"
```

- [x] **Step 5: Barrel**

Criar `apps/web/core/components/clients/index.ts`:

```ts
/**
 * Copyright (c) 2023-present Plane Software, Inc. and contributors
 * SPDX-License-Identifier: AGPL-3.0-only
 * See the LICENSE file for details.
 */

export * from "./client-detail-root";
export * from "./client-form-modal";
export * from "./clients-list-root";
export * from "./cnpj";
```

- [x] **Step 6: Verificação manual** _(pendente de confirmação do usuário no browser; as telas só ficam legíveis depois da Task 18/i18n)_

```bash
pnpm dev --filter=web
```

Roteiro, em `http://localhost:3000/<slug>/clients`:

1. Criar cliente "Acme" com duas empresas ("Acme SP" `11.222.333/0001-81`, "Acme RJ" com outro CNPJ válido). Esperado: aparece na lista com Empresas = 2.
2. Tentar salvar uma empresa com CNPJ `11222333000182`. Esperado: erro inline "CNPJ inválido", modal não fecha.
3. Tentar criar outro cliente chamado "acme". Esperado: toast de erro sobre nome duplicado.
4. Abrir o detalhe do cliente. Esperado: as duas empresas listadas com CNPJ mascarado.
5. Excluir "Acme" (ainda sem tarefas vinculadas). Esperado: some da lista.

- [x] **Step 7: Verificar tipos e lint**

```bash
pnpm turbo run check:types --filter=web && pnpm turbo run check:lint --filter=web
```

Esperado: sem erros; a contagem de warnings do web não pode ultrapassar o teto atual (`--max-warnings=11957`).

- [x] **Step 8: Commit**

```bash
git add "apps/web/app/(all)/[workspaceSlug]/(projects)/clients" apps/web/app/routes/core.ts apps/web/core/components/clients/
git commit -m "feat(clientes): telas de lista, cadastro e detalhe de clientes"
```

---

## Fase 7 — Cliente como propriedade da tarefa

### Task 14: Dropdown de cliente

**Files:**

- Create: `apps/web/core/components/dropdowns/client.tsx`
- Modify: `apps/web/core/components/issues/issue-detail/sidebar.tsx`, `apps/web/core/store/issue/issue-details/issue.store.ts`

- [x] **Step 1: Ler o padrão existente** _(o dropdown seguiu o padrão do `dropdowns/cycle`: `ComboDropDown` + `DropdownButton` + `Combobox.Options` com `position: fixed` e `usePopper`, que resolve o mesmo problema do peek overview sem `createPortal`)_

```bash
cat apps/web/core/components/dropdowns/recurrence.tsx | head -80
```

O painel **precisa** usar `createPortal` + `react-popper`, não `Popover` do Headless UI: no peek overview (sidebar de 400px com `overflow-hidden`) o Popover salta para a borda direita da página. Pitfall registrado em `mods/tarefas-recorrentes.md`.

- [x] **Step 2: Implementar o dropdown**

Criar `apps/web/core/components/dropdowns/client.tsx` com:

- Props: `{ value: string | null; onChange: (clientId: string | null) => void; disabled?: boolean; buttonVariant?: TButtonVariants; className?: string; placeholder?: string; showTooltip?: boolean }`.
- `observer`, usa `useClient()` e `useParams()`; garante `fetchClients` via `useSWR` na primeira abertura.
- Lista `activeClients` filtrada por um input de busca local; primeira opção é "Nenhum" (`onChange(null)`).
- Se o `value` atual apontar para um cliente inativo, esse cliente aparece no botão (lido de `getClientById`) mesmo não estando na lista de opções.
- Botão renderiza ícone `Building2` + nome do cliente, ou o placeholder quando `value` é nulo.

- [x] **Step 3: Exportar** _(não se aplica: `apps/web/core/components/dropdowns/` não tem barrel; os consumidores importam o caminho direto, como `@/components/dropdowns/cycle`)_

- [x] **Step 4: Ligar no sidebar da tarefa**

Localizar o sidebar de propriedades da issue:

```bash
grep -rn "CycleDropdown" apps/web/core/components/issues/issue-detail/sidebar.tsx | head
```

Adicionar uma linha de propriedade "Cliente" seguindo exatamente o mesmo formato do bloco do `CycleDropdown` vizinho (label à esquerda com ícone, dropdown à direita), chamando:

```tsx
<SidebarPropertyListItem icon={Building2} label={t("clients.client")}>
  <ClientDropdown
    value={issue?.client_id ?? null}
    onChange={(clientId) => issueOperations.update(workspaceSlug, projectId, issueId, { client_id: clientId })}
    disabled={!isEditable}
    buttonVariant="transparent-with-text"
    placeholder={t("clients.none")}
    hideIcon
    dropdownArrow
  />
</SidebarPropertyListItem>
```

- [x] **Step 5: `client_id` na allowlist do store de detalhe** _(antecipado na Task 10: o check:types do web nao fechava sem ele)_

Em `apps/web/core/store/issue/issue-details/issue.store.ts`, no objeto montado por `addIssueToStore`, adicionar junto de `cycle_id`:

```ts
      client_id: issue?.client_id,
```

Sem isso o dropdown mostra "Nenhum" mesmo com o valor salvo no banco — mesmo pitfall do `recurrence_pattern`.

- [x] **Step 6: Verificação manual** _(pendente de confirmação do usuário no browser)_

```bash
pnpm dev --filter=web
```

Abrir uma tarefa (página e peek). Esperado: linha "Cliente" no painel de propriedades; selecionar "Acme" persiste após recarregar a página; abrir no peek não desloca o painel do dropdown para a borda da tela.

- [x] **Step 7: Commit**

```bash
git add apps/web/core/components/dropdowns/client.tsx apps/web/core/components/dropdowns/index.ts apps/web/core/components/issues/issue-detail/sidebar.tsx apps/web/core/store/issue/issue-details/issue.store.ts
git commit -m "feat(clientes): dropdown de cliente no detalhe da tarefa"
```

### Task 15: Coluna "Cliente" na planilha

**Files:**

- Create: `apps/web/core/components/issues/issue-layouts/spreadsheet/columns/client-column.tsx`
- Modify: `apps/web/core/components/issues/issue-layouts/spreadsheet/columns/index.ts`, `apps/web/ce/components/issues/issue-layouts/utils.tsx`, `packages/constants/src/issue/common.ts`

- [x] **Step 1: Criar a coluna**

Criar `apps/web/core/components/issues/issue-layouts/spreadsheet/columns/client-column.tsx`, espelhando `cycle-column.tsx` (mesmas props `issue`, `onChange`, `disabled`, mesma casca de célula), renderizando o `ClientDropdown` com `buttonVariant="transparent-with-text"` e chamando `onChange(issue, { client_id: clientId }, ...)`.

```bash
cat apps/web/core/components/issues/issue-layouts/spreadsheet/columns/cycle-column.tsx
```

- [x] **Step 2: Exportar**

Em `apps/web/core/components/issues/issue-layouts/spreadsheet/columns/index.ts`, adicionar:

```ts
export * from "./client-column";
```

- [x] **Step 3: Registrar coluna e ícone**

Em `apps/web/ce/components/issues/issue-layouts/utils.tsx`:

```tsx
  client: SpreadsheetClientColumn,
```

no objeto `SPREADSHEET_COLUMNS`, e

```tsx
  ClientIcon: Building2,
```

em `SpreadSheetPropertyIconMap` (importando `Building2` de `lucide-react`).

- [x] **Step 4: Registrar a display property e a coluna nos constants**

Em `packages/constants/src/issue/common.ts`:

`ISSUE_DISPLAY_PROPERTIES_KEYS` — adicionar `"client"` no fim.

`ISSUE_DISPLAY_PROPERTIES` — adicionar:

```ts
  { key: "client", titleTranslationKey: "clients.title" },
```

`SPREADSHEET_PROPERTY_LIST` — adicionar `"client"` depois de `"cycle"`.

`SPREADSHEET_PROPERTY_DETAILS` — adicionar:

```ts
  client: {
    // Ordenação server-side por nome de cliente não é suportada ainda;
    // ambas as chaves caem no sort manual, deixando o menu do header inerte.
    i18n_title: "clients.title",
    ascendingOrderKey: "sort_order",
    ascendingOrderTitle: "Manual",
    descendingOrderKey: "sort_order",
    descendingOrderTitle: "Manual",
    icon: "ClientIcon",
  },
```

- [x] **Step 5: Verificação manual** _(pendente de confirmação do usuário no browser)_

```bash
pnpm dev --filter=web
```

Abrir um projeto no layout Planilha. Esperado: coluna "Clientes" presente, editável inline, aparecendo no menu de Propriedades de exibição e arrastável (reordenamento persistente do fork).

- [x] **Step 6: Commit**

```bash
git add apps/web/core/components/issues/issue-layouts/spreadsheet/columns/ apps/web/ce/components/issues/issue-layouts/utils.tsx packages/constants/src/issue/common.ts
git commit -m "feat(clientes): coluna Cliente na planilha"
```

### Task 16: Filtro por cliente

**Files:**

- Modify: `apps/web/core/store/issue/helpers/base-issues.store.ts`, `packages/constants/src/issue/common.ts`, componentes de filtro

- [x] **Step 1: Mapear o filtro para a propriedade da issue**

Em `packages/constants/src/issue/common.ts`, em `FILTER_TO_ISSUE_MAP`, adicionar:

```ts
  client: "client_id",
```

- [x] **Step 2: Adicionar a opção de filtro na UI** _(desvio: `components/issues/filters/filters-selection/` não existe mais — o par `filters/header/filters/cycle.tsx` + `applied-filters/cycle.tsx` virou código órfão depois da migração para **rich filters**. O filtro foi implementado no caminho atual)_

O caminho atual é um registro de configs: `WorkItemFiltersHOC` → `useWorkItemFiltersConfig` → factories `getXFilterConfig` de `@plane/utils`. O chip do filtro aplicado é genérico (`components/rich-filters/filters-row`), então não precisa de componente próprio.

Pontos tocados:

1. `packages/types/src/view-props.ts` — `"client_id"` em `WORK_ITEM_FILTER_PROPERTY_KEYS`, e `"client"` em `TIssueParams`.
2. `packages/utils/src/work-item-filters/configs/filters/client.ts` (novo, espelhando `cycle.ts`) + export no `index.ts` da pasta.
3. `apps/web/ce/hooks/work-item-filters/use-work-item-filters-config.tsx` — `clientFilterConfig` (ícone `Building2`), entrada no array `configs` e em `configMap`. **Clientes são de workspace**, então a lista vem direto de `useClient().activeClients` em vez de ser plumbada por `project-level.tsx` / `workspace-level.tsx` como acontece com ciclos.
4. `packages/constants/src/issue/filter.ts` — `"client_id"` nos arrays `filters` de `archived_issues`, `issues` e `my_issues`; `"client_id" = "client"` em `EServerGroupByToFilterOptions`.
5. Backend: `apps/api/plane/utils/filters/filterset.py` (`client_id` / `client_id__in`, FK simples, sem method custom) e `apps/api/plane/utils/filters/converters.py` (`"client": "client_id"` + `"client_id"` em `DEFAULT_UUID_FIELDS`).

- [x] **Step 3: Verificação manual** _(pendente de confirmação do usuário no browser)_

```bash
pnpm dev --filter=web
```

Em um projeto, abrir Filtros → Cliente → selecionar "Acme". Esperado: lista reduz às tarefas do cliente; o chip "Cliente: Acme" aparece; recarregar a página mantém o filtro; salvar como view e reabrir mantém o filtro.

- [x] **Step 4: Commit**

```bash
git add packages/constants/src/issue/common.ts apps/web/core/components/issues/filters/
git commit -m "feat(clientes): filtro de tarefas por cliente"
```

### Task 17: Agrupamento por cliente

**Files:**

- Modify: `packages/constants/src/issue/common.ts`, `packages/types/src/view-props.ts`, `apps/web/core/store/issue/helpers/base-issues.store.ts`, `apps/web/core/components/issues/issue-layouts/utils.tsx`

- [x] **Step 1: Adicionar `client` ao tipo de group by** _(além de `TIssueGroupByOptions`, foi preciso `GroupByColumnTypes` em `packages/types/src/issues.ts`, `TIssueParams`, e as enums `EIssueGroupByToServerOptions` / `EIssueGroupBYServerToProperty` / `EServerGroupByToFilterOptions` — sem elas o `issue-filter-helper.store.ts` não compila)_

Em `packages/types/src/view-props.ts`, adicionar `"client"` à união `TIssueGroupByOptions` (e, se existir, também a `TIssueDisplayFilterOptions`).

- [x] **Step 2: Adicionar a opção no menu** _(além de `ISSUE_GROUP_BY_OPTIONS`, `"client"` entrou nos arrays `group_by`/`sub_group_by` de `ISSUE_DISPLAY_FILTERS_BY_PAGE`, que é quem gateía o menu por página)_

Em `packages/constants/src/issue/common.ts`, em `ISSUE_GROUP_BY_OPTIONS`, adicionar antes do item `{ key: null, ... }`:

```ts
  { key: "client", titleTranslationKey: "clients.title" },
```

- [x] **Step 3: Mapear a chave para a propriedade da issue**

Em `apps/web/core/store/issue/helpers/base-issues.store.ts`, adicionar `client: "client_id",` **nos dois** mapas — `ISSUE_GROUP_BY_KEY` e `ISSUE_FILTER_DEFAULT_DATA`. São `Record<TIssueDisplayFilterOptions, keyof TIssue>`, então omitir um deles quebra a compilação.

- [x] **Step 4: Construir as colunas do agrupamento**

Em `apps/web/core/components/issues/issue-layouts/utils.tsx`, adicionar a função e registrá-la em `groupByColumnMap`:

```tsx
const getClientColumns = (): IGroupByColumn[] | undefined => {
  const { activeClients } = store.client;
  const columns: IGroupByColumn[] = activeClients.map((client) => ({
    id: client.id,
    name: client.name,
    icon: <Building2 className="size-3.5 flex-shrink-0" />,
    payload: { client_id: client.id },
  }));
  columns.push({
    id: "None",
    name: "Sem cliente",
    icon: <Building2 className="size-3.5 flex-shrink-0" />,
    payload: {},
  });
  return columns;
};
```

```tsx
    client: getClientColumns,
```

- [x] **Step 5: Verificação manual** _(pendente de confirmação do usuário no browser)_

```bash
pnpm dev --filter=web
```

Em um projeto no layout Kanban, Agrupar por → Cliente. Esperado: uma coluna por cliente ativo mais "Sem cliente"; arrastar um card entre colunas altera o cliente da tarefa e persiste após recarregar. Repetir no layout Lista.

- [x] **Step 6: Commit**

```bash
git add packages/types/src/view-props.ts packages/constants/src/issue/common.ts apps/web/core/store/issue/helpers/base-issues.store.ts apps/web/core/components/issues/issue-layouts/utils.tsx
git commit -m "feat(clientes): agrupamento de tarefas por cliente"
```

---

## Fase 8 — i18n, documentação e verificação final

### Task 18: Traduções

**Files:**

- Modify: todos os `packages/i18n/src/locales/<lang>/translations.json`

- [ ] **Step 1: Listar os locales**

```bash
ls packages/i18n/src/locales/
```

- [ ] **Step 2: Adicionar as chaves**

Em **cada** locale, adicionar o bloco `clients`. Em `pt-BR`, traduzido:

```json
  "clients": {
    "title": "Clientes",
    "client": "Cliente",
    "new_client": "Novo cliente",
    "edit_client": "Editar cliente",
    "name": "Nome do cliente",
    "name_is_required": "O nome do cliente é obrigatório.",
    "companies": "Empresas",
    "no_companies": "Nenhuma empresa cadastrada.",
    "company_name": "Nome da empresa",
    "company_name_is_required": "O nome da empresa é obrigatório.",
    "cnpj": "CNPJ",
    "add_company": "Adicionar empresa",
    "remove_company": "Remover empresa",
    "invalid_cnpj": "CNPJ inválido.",
    "duplicate_cnpj": "Este CNPJ já está cadastrado neste workspace.",
    "duplicate_name": "Já existe um cliente com esse nome neste workspace.",
    "issue_count": "Tarefas",
    "view_issues": "Ver {count, plural, one {# tarefa} other {# tarefas}}",
    "empty": "Nenhum cliente cadastrado ainda.",
    "not_found": "Cliente não encontrado.",
    "back_to_list": "Voltar para a lista de clientes",
    "show_inactive": "Mostrar inativos",
    "delete_blocked": "Este cliente está vinculado a tarefas. Inative-o em vez de excluir.",
    "deactivate": "Inativar",
    "none": "Nenhum",
    "status": {
      "label": "Status",
      "active": "Ativo",
      "inactive": "Inativo"
    }
  }
```

Nos demais locales, o mesmo bloco com os valores em inglês ("Clients", "New client", "Companies", "Tax ID", …), servindo de placeholder — é a convenção do repositório (ver `CLAUDE.md`).

- [ ] **Step 3: Verificar a sincronia**

```bash
pnpm turbo run check:types --filter=@plane/i18n
```

Esperado: sem erros. O sync check de i18n do fork só exige pt-BR (mod #11), mas as chaves precisam existir em todos os arquivos.

- [ ] **Step 4: Verificação manual**

```bash
pnpm dev --filter=web
```

Esperado: sidebar mostra "Clientes" (não mais a chave crua `clients.title`); modal, coluna e menu de agrupamento todos em português.

- [ ] **Step 5: Commit**

```bash
git add packages/i18n/src/locales/
git commit -m "feat(clientes): traducoes do modulo de clientes"
```

### Task 19: Registro em `mods.md`

**Files:**

- Create: `mods/clientes-cadastro.md`
- Modify: `mods.md`

- [ ] **Step 1: Escrever o detalhamento**

Criar `mods/clientes-cadastro.md` em português, seguindo `mods/notificacoes-web-push.md` como referência de formato. Seções obrigatórias: bloco de cabeçalho (`Data: 2026-09-03`, `Autor: felipedrn93`, `Branch: main`, `Plano: docs/superpowers/plans/2026-09-03-clientes.md`), **Contexto**, **Decisões de design** (as 9 do spec), **Arquivos criados**, **Arquivos modificados** (backend / frontend-tipos / frontend-constantes / frontend-store / frontend-componentes / i18n, como no `mods/parent-breadcrumb.md`), **Fluxo end-to-end**, **Como testar** e **Pitfalls**.

Nos Pitfalls, registrar explicitamente: (a) as shadow allowlists de campos de issue, (b) `client_id` é escalar e **não** entra no `FIELD_MAPPER` do grouper, (c) o item de sidebar precisa estar em `staticItems` além dos constants, (d) o dropdown precisa de portal+popper por causa do peek de 400px.

Cruzar com `[mods/tarefas-recorrentes.md](tarefas-recorrentes.md)` e `[mods/reordenar-colunas-spreadsheet.md](reordenar-colunas-spreadsheet.md)`.

- [ ] **Step 2: Adicionar a linha ao índice**

Em `mods.md`, adicionar a linha 15 ao final da tabela:

```
| 15  | 2026-09-03 | Módulo Clientes (cadastro com N empresas/CNPJ) + vínculo com tarefas                                         | [mods/clientes-cadastro.md](mods/clientes-cadastro.md)                                                           |
```

- [ ] **Step 3: Commit**

```bash
git add mods.md mods/clientes-cadastro.md
git commit -m "docs(clientes): registra o modulo de clientes em mods.md"
```

### Task 20: Verificação final

- [ ] **Step 1: Suíte do backend**

```bash
cd apps/api && python -m pytest -m unit --reuse-db -q
```

Esperado: tudo passando. Anotar o número de testes.

- [ ] **Step 2: Lint do Python**

```bash
cd apps/api && ruff check plane/ && ruff format --check plane/
```

Esperado: sem erros.

- [ ] **Step 3: Check completo do frontend**

```bash
pnpm check
```

Esperado: `check:format`, `check:lint` e `check:types` todos verdes. Se o lint do `web` estourar `--max-warnings`, corrigir os warnings novos — **não** elevar o teto.

- [ ] **Step 4: Roteiro manual end-to-end**

Com `pnpm dev` rodando:

1. Sidebar mostra "Clientes" logo abaixo de "Página Inicial".
2. Criar cliente "Acme" com duas empresas; criar "Globex" com uma.
3. Tentar cadastrar em "Globex" um CNPJ já usado por "Acme". Esperado: erro de CNPJ duplicado.
4. Em um projeto, vincular 3 tarefas a "Acme" e 1 a "Globex" pelo dropdown do detalhe.
5. Planilha: coluna "Clientes" mostra os vínculos; editar inline funciona.
6. Filtrar por Cliente = Acme. Esperado: 3 tarefas.
7. Agrupar por Cliente no Kanban. Esperado: colunas Acme (3), Globex (1), Sem cliente (resto). Arrastar entre colunas altera o vínculo.
8. Salvar uma view com o filtro de cliente; sair e reabrir. Esperado: filtro preservado.
9. Abrir `/clients/<id do Acme>`. Esperado: 2 empresas listadas e a aba Tarefas com as 3 tarefas.
10. Tentar excluir "Acme". Esperado: bloqueio com a mensagem de tarefas vinculadas e a opção de inativar.
11. Inativar "Acme". Esperado: some do dropdown de novas tarefas, mas continua exibido nas 3 tarefas que já o referenciam.
12. Entrar com um usuário Convidado. Esperado: vê a lista de clientes, mas sem o botão "Novo cliente" e sem menu de editar/excluir.

- [ ] **Step 5: Commit final e push**

```bash
git push origin main
```

- [ ] **Step 6: Deploy no CT 105**

Há migrations, então o `migrator` (imagem separada) precisa ser reconstruído — caso contrário o `api` fica travado em "Waiting for database migrations to complete..." e o proxy devolve 502.

```bash
ssh root@100.102.42.9
cd /opt/plane && git pull origin main
docker compose build api migrator web
docker compose up migrator
docker compose up -d api web
```

Verificar:

```bash
docker compose logs api | grep -i migrat
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/api/instances/
```

Esperado: `200` (o `api` leva ~30 s para subir o gunicorn; antes disso, 502 é normal).
